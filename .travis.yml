language: ruby
rvm:
  - "2.0.0"
before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
services: mongodb
bundler_args: --without test development
script: export DISPLAY=:99.0 && bundle exec teaspoon && bundle exec rake
notifications:
  email:
    recipients:
      - healthcare-ci@googlegroups.com
    on_failure: change
