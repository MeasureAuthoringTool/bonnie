# used with Care Goal; Communication, Provider to Patient; Communication,
# Patient to Provider; Communication, Provider to Provider; Assessment,
# Performed
describe 'RelatedToView', ->
  beforeEach ->
    @careGoal =
    @communicationPerformed =
    @assessmentPerformed =
  afterEach ->

  it 'is displayed for Care Goal', ->
  it 'is displayed for Communication, Performed', ->
  it 'is displayed for Assessment, Performed', ->