<div class="measure-diff">
  {{#if diffView}}
    <div class="row">
      <div class="col-md-6"><h3><strong>{{diff.pre_upload.cms_id}}</strong> @ {{diff.pre_upload.updateTime}}</h3><small>{{diff.pre_upload.hqmf_id}}</small></div>
      <div class="col-md-6 text-right"><h3><strong>{{diff.post_upload.cms_id}}</strong> @ {{diff.post_upload.updateTime}}</h3><small>{{diff.post_upload.hqmf_id}}</small></div>
    </div>
    <div class="measure-viz rationale panel-group">
      
    {{#each diffView}}
      <div class="panel panel-default">
        <div class="panel-heading">
          <h4 class="panel-title panel-population">
            <a data-toggle="collapse" href="#collapseDiff{{@index}}">
              <div>
                <span class="population-label rationale-target"><b>{{this.title}}: </b><span class="sr-only sr-highlight-status"></span></span>
                <div class="pull-right">
                  <i class="fa fa-lg fa-angle-down toggle-icon" aria-hidden="true"></i>
                  <span class="sr-only">toggle {{this.title}}</span>
                </div>
              </div>
            </a>
          </h4>
        </div>
        <div id="collapseDiff{{@index}}" class="panel-collapse collapse in">
          <div class="panel-body"><div class="row">
            <div class="col-md-6">
              {{{this.pre_upload}}}
            </div>
            <div class="col-md-6">
              {{{this.post_upload}}}
            </div>
          </div></div>
        </div>
      </div>
    {{/each}}
  {{else}}
    <p>Loading comparison...</p>
  {{/if}}
</div>
