{{#collection codes class="existing-values"}}
<span>
  {{system}}:
  {{code}}
  <button data-call-method="removeCode" class="btn btn-link close delete" code-index="{{index}}">&times; <span class="sr-only">Delete</span></button>
</span>
{{/collection}}