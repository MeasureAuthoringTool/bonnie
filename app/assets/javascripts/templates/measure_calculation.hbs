<input type="text" value="{{expectedPercentage}}" class="dial" data-readOnly="true" data-width="45" data-height="45" data-thickness=".1" data-fgColor={{#ifCond status '==' 'success'}}"#468847"{{/ifCond}}{{#ifCond status '==' 'failed'}}"#b94a48"{{/ifCond}} data-inputColor="#444" data-font="Helvetica Neue, Roboto, Helvetica, Arial, sans-serif">
{{status}}
<a href="#measures/{{_id}}/patients/new" class="btn {{#unless patients.length}}btn-primary{{else}}btn-default{{/unless}} pull-right">
  <i class="glyphicon glyphicon-user"></i>
  <i class="glyphicon glyphicon-plus"></i>
</a>
{{matches}}/{{patients.length}}
<h3>Select Patients</h3>
<div data-toggle="buttons-checkbox">
  {{#collection patients}}
    <button style="width: 180px; margin: 5px 5px 0px 0px;" class="btn btn-primary toggle-patient">{{last}}, {{first}}</button>
  {{/collection}}
</div>
<br>
{{#collection results item-context=resultContext}}
  <h5>Results for {{populationTitle}}</h5>
  <table class="table table-striped table-bordered table-condensed col-xs-3">
    <tr>
      <th>{{last}}, {{first}}</th>
      <th>Expected</th>
      <th>Results</th>
    </tr>
    {{#each resultRow}}
      <tr>
        <td>{{this.name}}</td>
        <td>{{this.expected}}</td>
        <td class="{{this.name}}-{{../patient_id}}">{{#if this.result}}X{{/if}}</td>
      </tr>
    {{/each}}
    {{!-- <tr>
      <td>IPP</td>
      <td>{{expectedValues.IPP}}</td>
      <td class="ipp-{{patient_id}}">{{#if IPP}}X{{/if}}</td>
    </tr>
    <tr>
      <td>DENOM</td>
      <td>{{expectedValues.DENOM}}</td>
      <td class="denom-{{patient_id}}">{{#if DENOM}}X{{/if}}</td>
    </tr>
    <tr>
      <td>NUMER</td>
      <td>{{expectedValues.NUMER}}</td>
      <td class="numer-{{patient_id}}">{{#if NUMER}}X{{/if}}</td>
    </tr>
    <tr>
      <td>DENEX</td>
      <td>{{expectedValues.DENEX}}</td>
      <td class="denex-{{patient_id}}">{{#if DENEX}}X{{/if}}</td>
    </tr>
    <tr>
      <td>DENEXCEP</td>
      <td>{{expectedValues.DENEXCEP}}</td>
      <td class="denexcep-{{patient_id}}">{{#if DENEXCEP}}X{{/if}}</td>
    </tr> --}}
      {{!-- <td width="4%">{{#button "showDelete" class="btn btn-default"}}<i class="glyphicon glyphicon-minus-sign"></i>{{/button}}</td> --}}
  </table>
  {{#link "measures/{{measure_id}}/patients/{{patient_id}}/edit" class="btn btn-primary" expand-tokens=true}}EDIT{{/link}}
  {{#button "clonePatient" class="btn btn-primary"}}CLONE{{/button}}
  {{#button "deletePatient" class="btn btn-danger delete-{{patient_id}}" expand-tokens=true}}Delete{{/button}}
{{/collection}}
